services:
  green-demo-web:
    build: .
    container_name: green-demo-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-3000}:3000"
    volumes:
      - daily-cache:/app/.cache

volumes:
  daily-cache:
