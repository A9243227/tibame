[project]
name = "tibame"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "selenium>=4.44.0",
]

[tool.uv.workspace]
members = [
    "tibame",
]
